services:
  app:
    build: .
    container_name: wasap-scheduler
    # ports:
    #   - "3000"
    environment:
      - NODE_ENV=development
    volumes:
      - wasap-data:/app
    networks:
      - mynetwork
networks:
  mynetwork:
volumes:
  wasap-data:
